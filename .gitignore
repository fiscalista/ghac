# Secrets / credentials — must never be committed
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# Local dependencies (none expected here, but defensive)
node_modules/

# OS / editor junk
.DS_Store
.idea/
.vscode/
*.swp
*~
